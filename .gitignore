# Images to boot, obviously shouldn't be part of the repository.
*.iso
*.img

# Other junk that shouldn't be in the repository
.cache
compile_commands.json
__pycache__/
build/
builddir/

# I'm working on the documentation you probably don't wanna see the
# horrors that exist in there...
docs/

# Patches, probably should stay right the fuck where they are
# and I will commit directly to master :P
*.patch

# Autogenerated by Bochs, we don't want it though.
bx_enh_dbg.ini
